[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "thepackage"
version = "0.1.0"
description = "My RadioLab package"

[tool.setuptools.packages.find]
where = ["src"]